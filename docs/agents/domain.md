# Domain docs

This repository uses a single domain context.

Before planning or changing the discovery system, read:

- `CONTEXT.md` for the repository's canonical product language.
- Relevant decisions under `docs/adr/`.

Use glossary terms in issue titles, acceptance criteria, tests, and documentation. Surface any contradiction with an accepted ADR rather than silently overriding it.
