{
  "name": "labdotsa-skills",
  "version": "0.0.0",
  "private": true,
  "description": "Public agent skills and playbooks from labdotsa",
  "type": "module",
  "scripts": {
    "build": "npm run catalog && npm run site:build",
    "catalog": "node scripts/generate-catalog.mjs",
    "site:build": "node scripts/build-site.mjs",
    "site:watch": "node --watch --watch-preserve-output --watch-path=skills --watch-path=recipes --watch-path=website scripts/build-site.mjs",
    "test": "node --test",
    "validate": "bash scripts/validate-skills.sh && node scripts/generate-catalog.mjs --check && node scripts/build-site.mjs --check && node scripts/validate-site.mjs && npm test"
  },
  "license": "MIT"
}
