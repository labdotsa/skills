# Design QA

## Visual truth

The refinement review used the supplied screenshots as the source of truth. Browser-rendered implementation captures live under the untracked `.artifacts/design-qa/` directory; the original six use a 1440 × 900 CSS viewport, while the header refinement reproduces its 526px reference width exactly.

| Reference | Pixels | Requested correction | Same-canvas evidence |
| --- | ---: | --- | --- |
| Screenshot 1 | 1824 × 346 | Replace the clipped handoff container with a compact Markdown-style blockquote. | `comparison-1-handoff.png` |
| Screenshot 2 | 2256 × 1632 | Replace the oversized two-column requirement cards with a compact table. | `comparison-2-requirements.png` |
| Screenshot 3 | 708 × 534 | Remove the redundant Outcome panel and integrate useful metadata into the Recipe hero. | `comparison-3-recipe-details.png` |
| Screenshot 4 | 2446 × 576 | Complete the directory perimeter and corner treatment. | `comparison-4-directory.png` |
| Screenshot 5 | 2382 × 1034 | Establish the home hero as one reusable LAB composition. | `comparison-5-home-hero.png` |
| Screenshot 6 | 2390 × 698 | Bring the Recipe index hero onto the same composition, rhythm, and type scale. | `comparison-6-recipes-hero.png` |
| Screenshot 7 | 526 × 194 | Move the mobile trigger to the far edge, use shadcn-svelte Sidebar, and match LAB's production logo and typography. | `header-refinement/comparison-header.png` |
| Screenshot 8 | 2216 × 1054 | Remove the redundant Reference column and make Skill and Source the reference affordances. | `requirements-responsive/comparison-desktop.png` |
| Screenshot 9 | 1112 × 1352 | Replace the clipped narrow table with the earlier compact card structure. | `requirements-responsive/comparison-responsive.png` |
| Screenshot 10 | 356 × 270 | Replace the false TypeScript label on a prose prompt with its truthful Text language. | `code-languages/comparison-code-label.png` |
| Screenshot 11 | 2356 × 1400 | Move the Recipe Contents rail from the left to the other side of the reading canvas. | `recipe-contents-right/comparison-right-rail.png` |
| Screenshot 12 | 2364 × 1392 | Replace the oversized, vague Recipe hero with direct copy and LAB-style inline actions. | `hero-copy-cta/comparison-supplied-before-after.png` |
| Screenshot 13 | 2048 × 1365 | Add directional Skills/Recipes navigation, footer spacing, hover-revealed pillar rows, an indigo/category page atmosphere, and pointer affordances. | `tmp/design-qa/home-atmosphere.png`, `tmp/design-qa/catalog-row-hover.png`, and `tmp/design-qa/skill-design-atmosphere.png` |
| Screenshots 14–17 | 1500 × 696, 1500 × 512, 1456 × 746, and 1500 × 556 | Replace four visibly different hero compositions with one shared layout and responsive type contract. | `tmp/design-qa/unified-heroes/comparison.png` |
| Live lab.sa gradient | 1440 × 900 | Replace the strong centered radial glow with LAB's full-viewport, 6.7%-strength linear wash. | Browser comparison against `https://lab.sa/`, `/services/research`, and `/services/design` |
| Screenshots 18–20 | 1508 × 740, 1524 × 804, and 1562 × 634 | Keep every hero element in the leading two thirds, preserve an empty trailing third at large screens, and remove competing one-line installation renderers. | `tmp/design-qa/hero-leading-grid/all-comparison.png` |
| Screenshot 21 | 1560 × 350 | Make every Recipe record LAB indigo and reveal its leading rail from the side rather than vertically from the center. | Live browser inspection at `/recipes/` plus the computed-style regression in `test/e2e/foundation.spec.mjs` |
| Screenshot 22 | 1574 × 212 | Remove the doubled final-row bottom rule by making the parent list own inter-row separators. | Live browser inspection at `/recipes/` plus the zero-row-border regression in `test/e2e/foundation.spec.mjs` |
| Screenshot 23 | 1706 × 550 | Tighten the footer hierarchy, alignment, and vertical rhythm while removing duplicated service labels. | Live browser inspection plus the footer geometry regression in `test/e2e/foundation.spec.mjs` |
| Screenshot 24 | 1500 × 1686 | Eliminate competing accent colors and give every single- or multiline code panel a branded muted surface and header. | Live Copywriting Skill inspection plus the accent/code-panel regression in `test/e2e/foundation.spec.mjs` |
| Screenshot 25 | 1542 × 748 | Give Related Content one complete rounded parent border and sibling-only dividers without row-owned edge rules. | Live Copywriting Skill inspection plus the parent-perimeter regression in `test/e2e/foundation.spec.mjs` |
| Screenshot 26 | 1720 × 486 | Use LAB's live footer slogan and stack Work with LAB services vertically instead of splitting them into two columns. | Live browser inspection against `https://lab.sa/` plus the footer copy/layout regression in `test/e2e/foundation.spec.mjs` |
| Screenshot 27 | 1508 × 1156 | Reduce the visual weight of repeated Recipe install panels while keeping their language labels and copy actions explicit. | `tmp/design-qa/requirements-polish/comparison-source-before-after.png` |
| Screenshot 28 | 1532 × 156 | Make the search field occupy its complete toolbar cell instead of nesting a rounded field inside padded chrome. | `tmp/design-qa/search-cell/comparison-source-before-after.png` |
| Screenshot 29 | 874 × 748 | Make every embedded Recipe install command become the Install or Use cell instead of another rounded panel inside it. | `tmp/design-qa/requirements-flush/comparison-source-before-after.png` |
| Screenshot 30 | 1018 × 168 | Wrap long lines inside multiline code fences instead of clipping or requiring horizontal scrolling. | `tmp/design-qa/code-wrap/comparison-source-before-after.png` |
| Screenshot 31 | 872 × 656 | Remove repeated Shell/Codex labels and the separately boxed copy column so each installation reads as one inline-code cell. | `tmp/design-qa/command-inline-copy/comparison-source-before-after.png` |
| Screenshot 32 | 1648 × 222 | Make the home directory, filters, records, and surrounding shell interpolate with the same light/dark timing as every other surface. | `tmp/design-qa/theme-transition-audit/directory-light-dark.png` |
| Screenshot 33 | 187 × 42 | Change detail breadcrumbs from `Subject > Sub-subject` to the requested `> Subject > Sub-subject` structure. | `tmp/design-qa/detail-breadcrumb/comparison.png` |
| Screenshots 34–35 | 119 × 64 and 171 × 64 | Put the LAB mark and label, navigation labels, appearance icon, and active rule on one true vertical centerline. | Live computed-geometry inspection plus the shared header/wordmark regression in `test/e2e/foundation.spec.mjs` |
| Screenshot 36 | 113 × 55 | Remove the redundant visible Installation segment from hero single-line commands without changing the command, copy action, or accessible group name. | Live Tailwind Skill inspection plus the shared hero-install regression in `test/e2e/foundation.spec.mjs` |
| Screenshots 37–38 | 827 × 189 and 827 × 130 | Remove the rectangular accent wash at the collapsed edge and let content disappear into the real branded surface in both themes. | Live Tailwind Skill capture plus the shared overflow-mask regression in `test/e2e/skill-detail.spec.mjs` |
| Screenshot 39 | 154 × 73 | Give the compact flask and LAB label identical 32px boxes and center both on one shared flex axis. | Live mobile Sidebar capture plus the compact lockup regression in `test/e2e/foundation.spec.mjs` |
| Screenshots 40–41 | 32 × 95 and 10 × 95 | Let the hero command's outer surface own the rounded perimeter while its inner single-line code row remains square and clips cleanly into the outer bottom corner. | Shared home/Skill hero command regression in `test/e2e/foundation.spec.mjs` |
| Screenshot 42 | 170 × 96 | Align the production footer flask and LAB wordmark through equal-height child boxes and one shared flex center, correcting the visible glyph center inside the label box. | Built-in browser capture and production-lockup geometry regression in `test/e2e/foundation.spec.mjs` |

Each comparison places the supplied reference above the implementation, separated by a LAB research-blue rule. Focused implementation crops were used for the handoff, requirements, Recipe metadata, directory, and header comparisons; the hero comparisons use complete viewport captures.

For Screenshot 11, the source visual truth is `/var/folders/sl/87qshknd4gz8bg33zgm4g_300000gn/T/TemporaryItems/NSIRD_screencaptureui_tclfKf/Screenshot 2026-08-12 at 11.47.31 AM.png`; the browser-rendered implementation is `.artifacts/design-qa/recipe-contents-right/implementation-desktop-1178x700.png`. The 2356 × 1400 Retina source was normalized to its 1178 × 700 CSS canvas and compared with a 1178 × 700 implementation capture at 1× density. The complete visible reading canvas is the important comparison region, so no smaller focused crop was needed.

For Screenshot 12, the supplied 2364 × 1392 Retina capture was normalized to its 1182 × 696 CSS canvas and compared with `.artifacts/design-qa/hero-copy-cta/implementation-recipes-1182x696.png` at 1× density. The live [lab.sa](https://lab.sa/) hero was also captured at a 1280 × 720 CSS viewport and placed with the same-size implementation in `hero-copy-cta/comparison-lab-pattern.png`. The full hero contains readable typography and actions at this size, so no additional focused crop was required.

For Screenshot 27, the source visual truth is `/var/folders/sl/87qshknd4gz8bg33zgm4g_300000gn/T/TemporaryItems/NSIRD_screencaptureui_CNyxf3/Screenshot 2026-08-12 at 2.22.04 PM.png` at 1508 × 1156. The browser-rendered implementation is `tmp/design-qa/requirements-polish/implementation-dark.png`, captured in dark mode at a 1910 × 1075 CSS viewport and 1× output density. Its 1216 × 596 requirements-table region was normalized to 1508px width and placed directly beneath the source in `tmp/design-qa/requirements-polish/comparison-source-before-after.png`. The complete table is the focused comparison region because it contains the typography, spacing, borders, language labels, commands, and copy controls under review. The initial P1 was the repeated two-level panel height and visual dominance; the post-fix comparison shows one compact horizontal command surface per row, and the copy control returned its visible success state in the in-app browser.

For Screenshot 28, the source visual truth is `/var/folders/sl/87qshknd4gz8bg33zgm4g_300000gn/T/TemporaryItems/NSIRD_screencaptureui_YyMFpf/Screenshot 2026-08-12 at 2.43.14 PM.png` at 1532 × 156. The browser-rendered implementation is `tmp/design-qa/search-cell/implementation-dark.png`, captured in dark mode at a 1910 × 1075 CSS viewport and 1× output density; its 1215 × 65 search row was normalized to 1530px width for `tmp/design-qa/search-cell/comparison-source-before-after.png`. The focused toolbar region is sufficient because no imagery or surrounding typography changed. Browser geometry confirms the input and owning cell share the exact x, y, width, and height, search filtering remains live, and the document has zero horizontal overflow.

For Screenshot 29, the source visual truth is `/var/folders/sl/87qshknd4gz8bg33zgm4g_300000gn/T/TemporaryItems/NSIRD_screencaptureui_twAwP8/Screenshot 2026-08-12 at 2.44.22 PM.png` at 874 × 748. The browser-rendered implementation is `tmp/design-qa/requirements-flush/implementation-dark.png`, captured at a 1910 × 1075 CSS viewport and 1× output density; its 1216 × 564 requirements table was normalized to 874px width for `tmp/design-qa/requirements-flush/comparison-source-before-after.png`. The table is the focused comparison region because the requested change affects cell ownership, borders, radii, spacing, labels, commands, and copy affordances. Browser geometry confirms a zero-padding owning cell, a zero-border/zero-radius command surface, sub-pixel edge parity, functional copy success, and zero page overflow.

For Screenshot 30, the source visual truth is `/var/folders/sl/87qshknd4gz8bg33zgm4g_300000gn/T/TemporaryItems/NSIRD_screencaptureui_XWm7Zo/Screenshot 2026-08-12 at 2.45.04 PM.png` at 1018 × 168. The browser-rendered implementation is `tmp/design-qa/code-wrap/implementation-dark.png`, captured in dark mode at a 1910 × 1075 CSS viewport and 1× output density; its focused 850 × 170 panel crop was placed beneath an 850px normalized source in `tmp/design-qa/code-wrap/comparison-source-before-after.png`. The code panel is the complete comparison region because only multiline content flow changed. Browser geometry confirms `white-space: pre-wrap`, `overflow-wrap: anywhere`, equal 807px client and scroll widths at the reference viewport, preserved source text, and zero page overflow.

For Screenshot 31, the source visual truth is `/var/folders/sl/87qshknd4gz8bg33zgm4g_300000gn/T/TemporaryItems/NSIRD_screencaptureui_wp9iOy/Screenshot 2026-08-12 at 2.57.29 PM.png` at 872 × 656. The browser-rendered implementation is `tmp/design-qa/command-inline-copy/implementation-dark.png`, captured in dark mode at a 1450 × 1601 CSS viewport and 1× output density; its focused 1218 × 565 requirements-table region was normalized to 872px width for `tmp/design-qa/command-inline-copy/comparison-source-before-after.png`. Browser inspection confirms zero language labels, a direct-child copy button on every installable command, transparent copy-button backgrounds, zero button/panel borders, successful clipboard output, and zero page overflow.

## States and interactions checked

- Desktop light home, Skills directory, Recipe index, Recipe detail hero, compact requirements table, and in-document handoff at 1440 × 900.
- Recipe detail at 390px: the shared hero stacks, the requirements table scrolls within its own frame, and the page itself has no horizontal overflow (`scrollWidth` and `clientWidth` both 390px).
- Home and Recipe index now expose one `data-lab-hero` composition with the same accent rail, eyebrow, display-title scale, description rhythm, and optional right-side action slot.
- Recipe detail no longer repeats the generic Outcome label; its bounded details panel keeps phase, Skill, author, and source information together.
- Every handoff is a semantic `blockquote` with one complete accent rail and no decorative half-border.
- Recipe requirements render as one semantic table with eight rows, row-level copy actions, an explicit built-in state, and a locally scrollable command region.
- Skills and Recipe directories use complete rounded borders with clipped children, so search, filters, and entries read as one bounded workbench.
- Light/dark appearance, global Skills/Recipes navigation, search, filters, mobile navigation, copy actions, and deep Recipe contents remain wired to the existing interaction model.
- The mobile header was measured at 526px: the flask begins at 32px, the 44px trigger ends 32px from the far edge, and the document has zero horizontal overflow.
- Mobile navigation renders through shadcn-svelte `Sidebar.Provider`, `Sidebar.Root`, header/content/group/menu/footer primitives, and its mobile Sheet boundary. Opening gives focus to Close navigation; closing restores focus to Open navigation.
- At 1280px the mobile Sidebar is removed from layout, the desktop navigation is visible, and the document remains exactly 1280px wide.
- Display text resolves to the locally bundled Maax Unicase family; body text resolves to the locally bundled IBM Plex Sans Arabic family. The header uses LAB's standalone production flask mark rather than the previous duplicate LAB lockup.
- At 2216 × 1054, Recipe requirements render as a four-column table with no Reference column. Both Skill and Source point to the same reference URL, open a new tab with `noopener noreferrer`, and expose a 2px primary underline plus color shift on hover/focus.
- At 1112 × 1352 the table swaps to compact two-column cards; at 390 × 844 it becomes one column. Long commands scroll inside their own focusable region while the page reports zero horizontal overflow at both widths.
- Browser diagnostics contained only Vite connection messages and no warnings or errors.
- All six multiline Recipe prompt panels now render the `TEXT` label; the Recipe contains no false Swift, VB.NET, Perl, C#, or TypeScript labels. The page remains overflow-free at 390px and browser diagnostics contain no warnings or errors.
- At the 1178 × 700 normalized desktop viewport, the Recipe reading column occupies the left grid track and the 240px sticky Contents rail occupies the right grid track with a 64px gutter. At 390 × 844, Contents remains before the article in the single-column flow; both regions measure 358px and the page has zero horizontal overflow.
- Selecting Planning from the moved rail updates the hash, current-location state, and keyboard focus. A fresh browser pass contained no warnings or errors.
- The Skills and Recipe collection heroes now share literal H1 copy, one short search-intent description, and an inline two-action zone. Visible descriptions exactly match their meta, Open Graph, Twitter, and structured-data projections through one typed copy source.
- Desktop actions are adjacent and 48px high. At 390 × 844 they stack at 334px wide inside the accent rail, remain 48px high, and create no horizontal overflow. The primary Recipe action reaches `#recipe-catalog`; fresh browser diagnostics contain no warnings or errors.
- The permanent hero rail is gone. Home and Recipe surfaces now fade from LAB indigo into the canvas from the top of the shell; Skill details inherit the owning pillar color, verified on the Design Skill page.
- Skills → Recipes uses a forward direction-aware View Transition and Recipes → Skills uses its inverse; the shared header and footer remain outside the named transition, and reduced-motion users receive an immediate route change.
- Directory and related rows rest without a category rail. Pointer hover and keyboard focus reveal the full-height pillar rail and a 10% same-pillar surface tint together; global links, enabled buttons, selects, labels, and summary controls expose a pointer cursor.
- The shared main landmark contributes 64px mobile and 96px desktop padding before the footer, including routes whose final section has no route-owned bottom margin.
- All four hero families now render the same ordered anatomy: optional breadcrumb prelude, eyebrow, a `14ch` display-title measure, then a support grid containing description/actions and an optional supplemental panel. The title resolves to the same 80px/76px desktop metrics and 48px/45.6px mobile metrics on every route, while content length alone determines wrapping and height.
- The supplemental column is optional without creating an alternate hero: home uses it for installation, Skill detail for installation/source, Recipe detail for metadata/source, and Recipe index simply retains the same primary support column. All four routes remain overflow-free at 390px.
- The page atmosphere now uses the same `100vh` top-to-bottom linear gradient as lab.sa. Default pages resolve to LAB indigo at 6.7% strength; Research and Design pages were verified against lab.sa's category-specific cyan and yellow washes at the same opacity.
- At `lg+`, every hero resolves to the same three equal tracks: the leading region spans tracks one and two, contains every populated hero region, and the third track has no children. Below `lg`, the third track is `display: none`, the grid becomes one column, and all four hero families remain overflow-free.
- Home collection installation and Skill installation now render through the same `InstallCommand` component with the same panel, label, command line, prompt, copy control, radius, border, and spacing; only content, accent, and the optional source link vary.
- Recipe records override category-derived accents with LAB indigo in both directory and related-content rows. Their full-height rail starts one rail-width beyond the leading edge and translates horizontally into place on hover or keyboard focus, with no center-origin vertical growth.
- Directory and related records no longer own bottom borders. Their parent lists create separators only between siblings, leaving the enclosing surface as the sole owner of the final bottom perimeter.
- The footer now uses a balanced `6 / 2 / 4` twelve-column desktop grid, a tighter brand block, and one quiet legal row; repeated Research, Design, Development, and Marketing labels were removed from the bottom edge because the service navigation already exposes them.
- Skill and Recipe pages now promote their owning `--page-accent` into the primary, focus, selection, navigation, and code-surface tokens. Standalone single-line commands and multiline fences share one `CodePanelHeader`, and every code panel uses a muted mix of the owning brand color instead of a second global accent.
- Related Content now matches the directory ownership contract: its list owns a complete one-pixel rounded perimeter and clipping, `divide-y` creates only sibling separators, and every rendered row reports a zero-width bottom border.
- The footer now repeats LAB's live product-innovation slogan verbatim. Research, Design, Development, and Marketing share one left edge and four distinct vertical positions under Work with LAB at desktop and mobile sizes.
- Recipe requirements now use one compact horizontal command anatomy: a persistent Shell/Codex label, a single-line command, and a 32px stateful copy control. Circular indices, a quieter header, parent-owned dividers, and row-level brand hover/focus feedback improve scanning without sacrificing the semantic table or responsive cards.
- The directory search field now is the toolbar cell: the icon, native search input, shortcut hint, and inset focus ring fill one uninterrupted 64px desktop surface. The live result count remains a separate divided cell and moves below the search surface at narrow widths.
- Each Recipe installation command now is its Install or Use cell. The parent table/card owns the perimeter and sibling dividers; the shared command component contributes only its language segment, command text, copy action, branded fill, and inset focus state.
- Every rich-content multiline fence now wraps prose, URLs, and long commands within its panel while retaining source newlines, indentation, language headers, branded surfaces, and copy actions. Inline code and single-line installation controls retain their separate compact behavior.
- Recipe installation cells no longer repeat Shell or Codex as leading columns. Installable rows render the prompt, command, and direct borderless copy action on one uninterrupted branded surface; the built-in Codex row retains only its actual instruction.
- Hero installation panels now render as one 56px horizontal row: the Installation label, prompt, no-wrap command, and copy action share one branded surface, while long commands overflow only inside the command track.
- Light and dark modes now share one 180ms interpolation contract across the shell, Recipe requirement parent, table, command cells, text, borders, icons, and copy controls; reduced-motion users retain the global near-instant override.
- Theme changes now expose one short-lived document transition lifecycle. During that lifecycle, the header, heroes, directory search/count/filter cells, records, Recipe requirements, Skill content, code panels, and footer all use the same 180ms color/background/border/icon/shadow property set and easing; component-specific hover and disclosure motion resumes afterward.
- Recipe and Skill detail pages now share one `DetailBreadcrumb` composition built from the shadcn-svelte breadcrumb primitives. Its visual order is consistently leading separator, linked subject, separator, current sub-subject, while the hidden separators remain excluded from the accessible breadcrumb name.
- The footer now applies only the relevant lab.sa pattern: a compact LAB lockup and product-innovation statement, one Discovery column, one vertically stacked LAB-services column, and a quiet legal row. Corporate Company/Legal groups, social links, language controls, and a duplicate theme control remain intentionally absent; the surface follows the site's active light or dark theme.
- The footer's LAB pillar strip now visualizes the published Skill catalog instead of four hard-coded equal quarters. Its fixed LAB order remains Research, Design, Development, Marketing, while each segment's flex weight is the number of stable Skills mapped to that pillar; the current `2 / 1 / 1 / 2` distribution therefore gives Research blue and Marketing green twice the width of Design yellow and Development coral.

## Fidelity findings

| Severity | Difference found | Correction | Post-fix evidence |
| --- | --- | --- | --- |
| P1 | Handoff containers looked clipped because their borders terminated at the viewport crop. | Replaced the box treatment with a semantic, Markdown-style blockquote and one deliberate accent rail. | `comparison-1-handoff.png` |
| P1 | Eight Recipe requirements consumed several screens as large cards. | Consolidated the same data and actions into a compact, accessible table. | `comparison-2-requirements.png` |
| P1 | The standalone Outcome block repeated the page title and weak metadata. | Removed it and added a compact, fully bordered Recipe details panel inside the shared hero. | `comparison-3-recipe-details.png` |
| P1 | Directory surfaces mixed open edges and rounded children. | Moved the radius, border, background, and clipping to the owning workbench containers. | `comparison-4-directory.png` |
| P1 | Home, Skill, Recipe index, and Recipe detail heroes used unrelated layout rules. | Added one composable `LabHero` with typed prelude and aside slots; all four page families now share its structure. | `comparison-5-home-hero.png` and `comparison-6-recipes-hero.png` |
| P2 | Earlier controls and content surfaces still carried generic or oversized patterns. | Retained the compact Package Content table, direct light/dark toggle, LAB footer, and route-owned Skills/Recipes navigation from the preceding refinement pass. | Browser review across home, Skill, and Recipe routes. |
| P1 | The mobile trigger sat immediately beside an oversized flask-and-LAB lockup, and navigation was composed directly from Sheet. | Adopted LAB's 96px production header rhythm, standalone flask mark, exact local display/body fonts, a far-edge trigger, and the shadcn-svelte Sidebar composition. | `header-refinement/comparison-header.png` and `implementation-sidebar-open.png` |
| P1 | A collapsed desktop rendering of the mobile-only Sidebar initially extended the scrollable document beyond the viewport. | Hid that instance at the desktop navigation breakpoint while retaining Sidebar's mobile Sheet behavior below it. | Browser measurements at 526px and 1280px both report zero overflow. |
| P1 | The separate Reference column duplicated information while Skill and Source looked inert. | Removed the column and made both labels direct external links with hover, focus, new-tab, and safe-rel behavior. | `requirements-responsive/implementation-table-hover-2216x1054.png` |
| P1 | The fixed-width table clipped its command column at the 1112px reference width. | Restored a responsive card composition below 1280px, using two columns at medium widths and one at phone widths. | `requirements-responsive/comparison-responsive.png` and `implementation-cards-390.png` |
| P1 | Prose prompts were fenced as unrelated programming languages, producing labels such as TypeScript. | Corrected every Recipe prompt fence to `text` and added a rendered-language marker plus regression assertion limiting Recipe panels to Text or Shell. | `code-languages/comparison-code-label.png` |
| P1 | The Recipe Contents rail occupied the left side while the requested reading composition called for it on the opposite side. | Reversed the wide-screen grid tracks without visual-order CSS: Contents remains first in the document at narrow widths and explicitly occupies the right track from the desktop breakpoint. | `recipe-contents-right/comparison-right-rail.png` |
| P1 | The Recipe collection headline wrapped into six oversized lines, its value was abstract, and its CTAs were detached at the lower right. | Reduced the shared display scale and widened its readable measure; replaced the copy with a literal agent-workflow intent; added a shared post-description action slot modeled on lab.sa's desktop/mobile hierarchy. | `hero-copy-cta/comparison-supplied-before-after.png` and `comparison-lab-pattern.png` |
| P1 | The permanent hero rail used research cyan on the default page, page backgrounds were flat, directory rails appeared before interaction, and the final section could touch the footer. | Removed the hero rail, added one page-level indigo/category gradient, synchronized the row rail and tint to hover/focus, and moved footer spacing into the shared shell. | `tmp/design-qa/home-atmosphere.png`, `tmp/design-qa/catalog-row-hover.png`, and `tmp/design-qa/skill-design-atmosphere.png` |
| P2 | Skills and Recipes snapped between route states and several custom clickable surfaces relied on the browser's contextual cursor. | Added a reduced-motion-safe, direction-aware View Transition and one global clickable-cursor contract. | Browser interaction regression plus computed-style assertions in `test/e2e/foundation.spec.mjs`. |
| P1 | Index heroes and detail heroes placed title, description, actions, and supplemental content using different internal grids and title measures. | Rebuilt the shared `LabHero` around one invariant DOM order and responsive contract, leaving only its typed content slots optional. | `tmp/design-qa/unified-heroes/comparison.png` and the four-route desktop/mobile contract regression in `test/e2e/foundation.spec.mjs`. |
| P1 | The Svelte shell used a 24%-strength centered radial ellipse, unlike both lab.sa and the retired HTML implementation's top-down linear wash. | Matched lab.sa's full-viewport linear geometry and exact 6.7% color strength while preserving category-specific accent selection. | Live computed-style inspection plus the atmosphere regression in `test/e2e/foundation.spec.mjs`. |
| P1 | Heroes still split supporting content into a populated trailing column, and the home and Skill installation lines used separate markup. | Replaced the split support grid with one `2/3 + empty 1/3` hero frame and routed both installation surfaces through `InstallCommand`. | `tmp/design-qa/hero-leading-grid/all-comparison.png` plus the geometry and shared-component regressions in `test/e2e/foundation.spec.mjs`. |
| P1 | Recipe records inherited cyan from their Product Delivery category, and their leading rails expanded vertically from the center. | Added a Recipe-level LAB indigo override and replaced center-origin scaling with a leading-edge horizontal translation in both row components. | Live `/recipes/` hover verification plus the computed-color and transform regression in `test/e2e/foundation.spec.mjs`. |
| P1 | Every record drew its own bottom rule, so the final Recipe row stacked a second line above the directory perimeter. | Removed row-owned bottom borders and applied sibling separators at the owning list level. | Live `/recipes/` inspection plus the `0px` final-row border regression in `test/e2e/foundation.spec.mjs`. |
| P2 | The footer had loose vertical rhythm and repeated all four service names beneath the service navigation, weakening its hierarchy. | Rebalanced the top content on a twelve-column grid, tightened spacing and copy measure, and reduced the meta row to its legal purpose. | Live browser inspection plus the footer geometry regression in `test/e2e/foundation.spec.mjs`. |
| P1 | Category pages mixed their owning pillar color with global cyan/indigo navigation, underline, focus, and code treatments; compact command blocks also lacked the multiline panel header. | Promoted the page accent into all interactive tokens, added one shared code-panel header, and mixed every code background from the same page color. | Live Copywriting Skill inspection plus the computed accent/header/background regression in `test/e2e/foundation.spec.mjs`. |
| P1 | Related Content used a top-only parent rule plus internal dividers, leaving square open edges and visually inconsistent line weight. | Replaced the top rule with one clipped rounded parent perimeter while retaining sibling-only dividers and borderless rows. | Live Copywriting Skill inspection plus the parent-perimeter regression in `test/e2e/foundation.spec.mjs`. |
| P2 | The local footer used a different slogan and split LAB services into two columns, unlike LAB's own footer hierarchy. | Adopted the live LAB slogan and changed the service navigation to a single vertical list. | Live `lab.sa` inspection plus the footer copy/layout regression in `test/e2e/foundation.spec.mjs`. |
| P1 | Repeating a full-width stacked code header in every Recipe requirement made the install column visually dominant and doubled each row's necessary height. | Collapsed the same shared header, command, and copy action into one 44px horizontal control; added quiet index markers and row-level hover/focus treatment. | `tmp/design-qa/requirements-polish/comparison-source-before-after.png` plus browser-verified copy success. |
| P2 | The search control was a rounded input inset inside another toolbar row, creating redundant padding and nested borders. | Removed the input's independent frame and made its label/input composition fill the complete search cell with an inset focus treatment. | `tmp/design-qa/search-cell/comparison-source-before-after.png` plus exact browser geometry and filtering checks. |
| P2 | Every embedded command remained a rounded, padded panel inside an already bounded Install or Use cell. | Added a flush mode to the shared command component and moved all perimeter/divider ownership to the table or responsive card. | `tmp/design-qa/requirements-flush/comparison-source-before-after.png` plus cell/panel geometry and copy-state checks. |
| P1 | Long lines in multiline Text and Shell fences were clipped beyond the panel edge and depended on horizontal scrolling. | Replaced the scroll region with preserved-whitespace wrapping and anywhere breaking on the shared rich-content code renderer. | `tmp/design-qa/code-wrap/comparison-source-before-after.png` plus desktop/mobile wrapping regressions. |
| P2 | Repeated Shell/Codex labels and a separately shaded copy column fragmented each compact installation cell into unnecessary visual compartments. | Removed the inline header segment and its copy wrapper; the copy button is now a transparent, borderless direct child of the shared command surface. | `tmp/design-qa/command-inline-copy/comparison-source-before-after.png` plus computed ownership/style and clipboard checks. |
| P2 | Hero installation commands used a stacked header and body, doubling the control height and diverging from the compact single-line command language used elsewhere. | Reused the shared inline code-panel header inside one three-track row, preserving the label, prompt, command, copy state, brand accent, and source link. | `tmp/design-qa/hero-inline-install/comparison-source-before-after.png` plus desktop/mobile component regressions. |
| P1 | Recipe requirement surfaces switched theme on mixed timing contracts: parent surfaces changed immediately, command cells used 200ms, and shadcn buttons used their separate default. | Added one shell-wide color/border/icon transition baseline and aligned the table row, command surface, and shared button duration to the same 180ms token and easing. | `tmp/design-qa/theme-transition/comparison-light-dark.png` plus live computed transition styles and desktop/mobile regressions. |
| P1 | The home directory still exposed component-local transition properties and easing during appearance changes, so its search, filters, and rows could visually lag the shell. | Added an explicit theme-change lifecycle that temporarily normalizes all rendered surfaces, then audited every color-aware Svelte component against the shared duration token. | `tmp/design-qa/theme-transition-audit/directory-light-dark.png` plus the home/Recipe/Skill desktop-mobile transition regression. |
| P2 | Detail breadcrumbs began directly with the subject and therefore omitted the requested leading directional marker. | Added the same shadcn-svelte separator before the first linked subject and consolidated both detail families onto one shared component. | `tmp/design-qa/detail-breadcrumb/comparison.png` plus the desktop/mobile structure regression. |
| P1 | The local footer forced a black campaign-style surface and oversized brand message, while copying lab.sa wholesale would have introduced unrelated corporate navigation and duplicate controls. | Applied only the relevant live hierarchy: compact LAB brand copy, Discovery links, a single vertical services list, theme-aware canvas, and one legal row. | Live `lab.sa` comparison plus desktop/mobile footer geometry, copy, theme, and overflow checks in `test/e2e/foundation.spec.mjs`. |
| P2 | The four-color footer rule allocated equal space to every LAB pillar even when the published Skill catalog did not. | Derived pillar counts from the canonical build snapshot and used those counts as the strip's proportional flex weights, omitting empty pillars without maintaining a duplicate list. | Unit projection coverage plus desktop/mobile assertions for the current `Research 2 / Design 1 / Development 1 / Marketing 2` catalog. |
| P1 | The footer LAB lockup used unequal 74.7px and 48px child boxes; matching their mathematical centers left the visible flask lower than the LAB glyphs. | Replaced image padding with equal 48px mark and label boxes, retained the shared flex center, and corrected the label's internal glyph position without translating either child. | Supplied 170 × 96 crop, built-in browser comparison, and production-lockup geometry regression in `test/e2e/foundation.spec.mjs`. |
| P2 | Single-line command copy actions rendered a smaller rounded hover target inside their full trailing cells. | Added a shared full-cell copy mode and applied it to hero and Recipe commands so hover, focus, and click feedback own the complete 48 px-wide trailing cell. | Supplied close-up plus desktop/mobile full-cell geometry and interaction regressions in `test/e2e/foundation.spec.mjs`. |
| P1 | Collapsed Skill instructions ended under a fixed near-black strip and hard top rule, disconnecting the disclosure from the page's current LAB pillar color. | Replaced the strip with a borderless transparent-to-surface gradient mixed from the active page accent, with a card-surface override for package tables. | Supplied Tailwind dark-mode capture plus four-pillar and light/dark computed-gradient regressions in `test/e2e/skill-detail.spec.mjs`. |
| P1 | The header used an inline home link and a real bottom border for active navigation, so the logo sat on a text baseline while the navigation labels were centered inside a box shortened by its underline. | Made the home link a full-height flex item and moved the active rule to an absolute overlay, leaving the mark, labels, and appearance glyph centered inside the same 96px header. | Supplied header crops plus the desktop geometry and lockup regressions in `test/e2e/foundation.spec.mjs`. |
| P2 | Hero installation commands repeated a visible Installation header inside an already-labeled single-line control. | Removed the visual title cell from the shared hero command while preserving its accessible Installation group name and direct copy action. | Supplied Tailwind crop plus the shared desktop/mobile hero-install regression. |
| P2 | The hero installation command inherited the full width of its two-column hero region, leaving a large empty code surface after the copy action. | Applied intrinsic `w-fit` sizing to the command root and surface with `max-w-full` as the narrow-screen cap. | Source: `/var/folders/sl/87qshknd4gz8bg33zgm4g_300000gn/T/TemporaryItems/NSIRD_screencaptureui_qkl1F8/Screenshot 2026-08-12 at 10.33.06 PM.png`; implementation: `tmp/design-qa/hero-command-fit/implementation-command.png`; combined comparison: `tmp/design-qa/hero-command-fit/comparison-before-after.png`. |
| P1 | The collapsed Skill edge painted a semi-transparent accent rectangle over the final lines, creating a hard band whose tint changed independently from the page surface. | Removed the painted overlay and masked the final 80px of content to transparency, revealing the actual branded page or card surface beneath it. | Supplied light/dark Tailwind crops, live computed mask inspection, and the desktop/mobile regression in `test/e2e/skill-detail.spec.mjs`. |
| P1 | The compact lockup used unequal image and text boxes, so a visual offset could not produce reliable structural alignment. | Removed the optical translation, gave both compact children explicit 32px heights, and centered them through the shared flex axis without changing production/footer geometry. | Supplied compact lockup crop plus mobile Sidebar computed-geometry regression. |
| P2 | The hero command grid owned the same rounded perimeter as its containing surface, leaving a doubled crescent where the branded leading rail met the corner. | Moved radius, perimeter border, background, and clipping to a dedicated outer surface; the inner single-line grid now has zero radius and only owns its branded leading rail. | Supplied corner crops plus the shared home/Skill hero command computed-style regression. |

## Verification

- `npm run typecheck`: zero errors and zero warnings.
- `npm test`: 79/79 passing.
- Focused multiline-wrapping Playwright regression: desktop and mobile passing; both assert preserved whitespace, anywhere wrapping, panel-contained width, and zero page overflow.
- Focused Recipe command-cell regression: desktop and mobile passing; both assert no language-label compartment, direct transparent/borderless copy ownership, full-cell geometry, successful copy state, and zero page overflow.
- Focused hero-install regression: desktop and mobile passing; home and Skill routes both resolve to the same three-track, 56px single-line panel with aligned centers, no-wrap commands, one copy action, and zero page overflow.
- In-app browser: the Information Architecture hero reports a 56px panel, three horizontal tracks, aligned 28px centers, no page overflow, and a successful copied state.
- Focused Recipe theme regression: desktop and mobile passing; section, table, command surface, and copy action all report `0.18s`, animate the relevant color/border properties, reach distinct light/dark values, and preserve zero page overflow.
- Focused codebase theme regression: desktop and mobile passing across Home, Recipe detail, and Skill detail; every representative shell, hero, directory, table, code, and footer surface reports the same `0.18s`/`ease` transition contract during the appearance lifecycle, the lifecycle clears afterward, and all routes preserve zero page overflow.
- In-app browser: the supplied home-directory region was captured in matched light and dark states at 1910 × 1074; search, result count, filters, row surface, enclosing perimeter, and surrounding shell all reach the correct paired theme with zero page overflow.
- Focused detail-breadcrumb regression: desktop and mobile passing for Recipe and Skill routes; both render exactly `separator → subject → separator → sub-subject`, retain one breadcrumb navigation landmark, and preserve zero page overflow.
- Focused footer regressions: desktop and mobile passing; the footer resolves to three desktop tracks and one mobile track, preserves at least 64px of preceding-page breathing room, keeps all four LAB services vertically aligned, remains below 480px on desktop, and introduces no duplicate service strap.
- Focused footer-distribution regression: desktop and mobile passing; the rendered strip reports six published Skills, exposes the canonical four-pillar order, and measures the Research and Marketing segments at exactly twice their Design and Development neighbors.
- Focused footer-wordmark regression: the production lockup uses a 32px-wide mark box and label box that are both exactly 48px tall, share one flex center, preserve the 48px/48px 700-weight Maax Unicase label and 1.2px tracking, and apply no image padding.
- Focused single-line copy regression: hero and Recipe copy buttons fill their complete trailing cells at desktop and mobile sizes while retaining clipboard success, focus visibility, shared branded hover treatment, and zero page overflow.
- Focused overflow-fade regression: collapsed Skill content uses a transparent mask rather than a painted overlay, revealing the owning branded page or card surface cleanly in light and dark themes.
- Focused header-alignment regressions: the LAB mark, Skills label, Recipes label, and appearance glyph share one computed centerline; compact lockup root, flask, and label are all exactly 32px tall with identical centers; production lockup mark and label centers are identical.
- Focused hero-install regressions: home and Skill commands render the same two-cell single-line control with no visible language/title header, one command, one full-cell copy action, and no overflow.
- In-app browser hero-command sizing: the 361.13px command is intrinsically sized inside a 768px desktop region; at the narrowest checked viewport it caps exactly to its 306.91px parent, and both states report zero document overflow. The source and implementation are normalized in `tmp/design-qa/hero-command-fit/comparison-before-after.png`.
- Focused hero-corner regression: the outer command surface owns the 8px rounded perimeter while the nested code grid reports a 0px radius and no block-edge borders on both home and Skill heroes.
- In-app browser: the Recipe detail breadcrumb visibly renders `> LAB Recipes > Functioning Prototype`; the normalized source/implementation comparison is saved at `tmp/design-qa/detail-breadcrumb/comparison.png`.
- In-app browser: the Recipe requirements transition contract reports 180ms for all four ownership layers, with matched light/dark captures and an observed intermediate frame; no content, radius, or divider geometry changes across themes.
- In-app browser: the reference-width panel reports equal 807px client and scroll widths, copy returns the complete 155-character source, and browser diagnostics contain no warnings or errors.
- `bash scripts/validate-skills.sh`: passed.
- `npm run catalog -- --check`: catalog current.
- `node scripts/validate-source-boundary.mjs`: passed.
- `node scripts/validate-dependencies.mjs`: passed.
- `npm run test:static`: canonical, preview, Pages-project, and Pages-root publications valid and structurally equivalent.
- `git diff --check`: passed.
- Svelte 5 autofixer reviewed the changed Svelte components; dynamic external URL notices were inspected rather than rewritten as internal routes.
- The current interaction pass also passed the four-profile publication matrix and the focused page-transition, row-hover, cursor, and footer-gap Playwright regressions.
- `npm run validate` passes structure, catalog, source/dependency boundaries, typechecking, 79 unit tests, 3 component tests, and the complete four-profile static matrix. Its E2E stage continues to be blocked by the branch's pre-existing assertion, accessibility, timing, and visual-baseline failures; the focused desktop/mobile alignment and overflow-fade regressions pass independently.

## Final findings

### Skill detail contents rail — 2026-08-12

- Source visual truth: the user-supplied missing-state capture at `/var/folders/sl/87qshknd4gz8bg33zgm4g_300000gn/T/TemporaryItems/NSIRD_screencaptureui_xmqzmX/Screenshot 2026-08-12 at 11.21.31 PM.png`, with the live Recipe detail contents rail used as the explicit existing-product reference.
- Implementation evidence: `.artifacts/skill-contents-implementation.png` at a 1440 × 1000 CSS viewport and 1× density; responsive evidence: `.artifacts/skill-contents-mobile.png` at 390 × 844 and 1× density.
- Normalized comparison: `.artifacts/contents-pattern-comparison.png` places the 1425 × 990 Recipe reference capture and the 1425 × 990 Skill implementation capture side by side at equal density and interaction state.
- Full-view comparison: passed. Both detail families use the same left reading track, 64px desktop gutter, 240px right contents rail, sticky header offset, active rail, primary/secondary hierarchy, and brand-owned accent.
- Focused region: the reading grid and contents rail are the complete changed region; no imagery or asset comparison was required.
- Fonts and typography: passed; the shared contents renderer preserves the existing LAB type families, sizes, weights, line heights, and section hierarchy.
- Spacing and layout rhythm: passed; browser geometry measured the Skill reading track at 912px and its right rail at 240px with no horizontal overflow. At 390px, both regions stack in document order without overflow.
- Colors and visual tokens: passed; the rail inherits the owning Skill's green primary token while the Recipe reference retains indigo.
- Image quality and asset fidelity: not applicable; the changed surface contains no images or nonstandard icons.
- Copy and content: passed; the rail is generated from the same immutable Skill outline as the rendered headings and introduces no hand-maintained labels.
- Interactions: passed; selecting `Workflow` updates the hash to `#content-workflow`, expands clipped instructions, focuses the target heading, and synchronizes current-location styling. Browser diagnostics contained no warnings or errors.
- Comparison history: the initial supplied state had no Skill contents navigation (P1). The fix extracted the Recipe behavior into one shared contents component and composed a Skill-specific outline adapter. The post-fix desktop/mobile captures show the same navigation contract with no remaining P0/P1/P2 difference.

### Hero command one-line truncation — 2026-08-12

- Source visual truth: `/var/folders/sl/87qshknd4gz8bg33zgm4g_300000gn/T/TemporaryItems/NSIRD_screencaptureui_YZxWBO/Screenshot 2026-08-12 at 11.22.18 PM.png` (303 × 94), showing an unwanted horizontal scrollbar inside the narrow command cell.
- Implementation evidence: `.artifacts/hero-command-truncate-mobile-clean.png` at a 390 × 844 CSS viewport and 1× density; focused command crop: `.artifacts/hero-command-truncate-mobile-crop.png`.
- Normalized comparison: `.artifacts/hero-command-truncate-comparison.png` places the 343px-wide normalized source above the 343px-wide implementation crop.
- Full-view comparison: passed; the hero remains within the narrow viewport and the copy cell retains its fixed full-height target.
- Focused comparison: passed; the command now ends in a visible ellipsis and the former scrollbar is absent.
- Fonts and typography: passed; the existing monospace family, size, line height, prompt, and one-line baseline are unchanged.
- Spacing and layout rhythm: passed; the command occupies a 234px client track inside the 343px panel and the page has no horizontal overflow.
- Colors and visual tokens: passed; the owning green Skill accent, muted code fill, perimeter, and copy hover surface are unchanged.
- Image quality and asset fidelity: not applicable; this control contains no raster imagery or custom icons.
- Copy and content: passed; the DOM, `title` attribute, and copy action retain the complete command while only visual overflow is truncated.
- Interaction: passed on a clean local runtime; copying reaches the `copied` state and browser diagnostics contain no warnings or errors.
- Comparison history: the supplied state exposed a nested horizontal scrollbar (P2). Replacing local scrolling with a one-line ellipsis clamp removes the scrollbar without changing the full command or copy behavior. No P0/P1/P2 difference remains.

### Responsive package contents list — 2026-08-12

- Source visual truth: `/var/folders/sl/87qshknd4gz8bg33zgm4g_300000gn/T/codex-clipboard-a774c2f9-7c66-49be-b247-f6e862614a9d.png` (313 × 300), showing the fixed-width package table and nested horizontal scrollbar at a phone width.
- Implementation evidence: `.artifacts/package-list-mobile-390.png` at a 390 × 844 CSS viewport and 1× density.
- Full-view comparison: passed; the package region now fits the mobile viewport without an internal or page-level horizontal scrollbar.
- Focused comparison: passed; the bordered parent still owns the radius while the tabular columns are replaced by one concise ordered file row.
- Fonts and typography: passed; file paths remain in the existing monospace family and retain the same weight and compact hierarchy.
- Spacing and layout rhythm: passed; the row uses a stable number track, a truncating file track, and one trailing external-link icon with no fixed minimum width.
- Colors and visual tokens: passed; the surface, perimeter, muted index, owning Skill accent, and hover/focus tint all use existing page tokens.
- Image quality and asset fidelity: not applicable; the only icon is the existing Lucide external-link glyph.
- Copy and content: passed; every canonical package file remains present in deterministic source order with its kind retained on wider screens.
- Interaction: passed; the whole row is the link, keyboard focus and pointer hover share the branded treatment, and activation targets the canonical GitHub file URL in a new tab with safe rel behavior.
- Comparison history: the supplied state exposed a narrow table viewport and nested scrollbar (P2). The responsive ordered-list composition removes the overflow and enlarges the interactive target without losing source order or metadata. No P0/P1/P2 difference remains.

### Skill contents hierarchy and collapsed-state synchronization — 2026-08-13

- Source visual truth: `/var/folders/sl/87qshknd4gz8bg33zgm4g_300000gn/T/TemporaryItems/NSIRD_screencaptureui_phlubC/Screenshot 2026-08-12 at 11.55.51 PM.png`, showing a flat, internally scrolling rail that listed headings hidden below the collapsed instruction excerpt.
- Implementation evidence: `.artifacts/skill-contents-collapsed-sync.png` at a 1220 × 985 CSS viewport and 1× density.
- Full-view comparison: passed; the Skill detail page keeps the existing Recipe-style reading/contents split and page-owned header offset.
- Focused comparison: passed; collapsed instructions now expose only `Copywriting` and `When to Use This Skill`, matching the headings actually visible in the excerpt.
- Hierarchy: passed; the source H1, H2, and H3 levels render at 16px, 28px, and 44px inline starts, and deeper descendants appear contextually beneath their active parent.
- Sticky behavior: passed; after scrolling the document to 1400px the rail remains pinned at 96px beneath the site header.
- Overflow: passed; the rail reports visible overflow with equal client and scroll heights, so the page—not an inner panel—owns vertical scrolling.
- Interaction: passed; expanding the instructions restores the complete outline, selecting a nested entry updates the hash, current state, focused heading, and branch disclosure, and reduced-motion navigation remains immediate.
- Comparison history: the supplied state had a long independent scrollbar, flattened headings, and hidden-section links in its collapsed state (P1). Shared hierarchical navigation, excerpt-aware filtering, and a non-scrolling sticky rail remove all three issues with no remaining P0/P1/P2 difference.

### Mobile Sidebar row and overflow correction — 2026-08-13

- Source visual truth: `/var/folders/sl/87qshknd4gz8bg33zgm4g_300000gn/T/TemporaryItems/NSIRD_screencaptureui_5qUhDy/Screenshot 2026-08-12 at 11.24.48 PM.png`, showing filled navigation rows and a horizontal scrollbar inside the open mobile Sidebar.
- Implementation evidence: `.artifacts/mobile-sidebar-fixed.png` at a 390 × 844 CSS viewport and 1× density.
- Full-view comparison: passed; the same shadcn-svelte Sidebar hierarchy, LAB lockup, section labels, footer placement, and close control are preserved.
- Focused comparison: passed; every navigation row is transparent at rest and the former bottom horizontal scrollbar is absent.
- Fonts and typography: passed; labels retain the existing LAB body face, weights, and compact hierarchy.
- Spacing and layout rhythm: passed; the sheet is capped inside the viewport, menu content measures 292px client and scroll width, and the shared inset separator measures 276px without exceeding its parent.
- Colors and visual tokens: passed; resting rows use the Sidebar surface while hover/focus underlines inherit the current page accent.
- Image quality and asset fidelity: passed; the supplied LAB lockup and existing Lucide icons remain unchanged.
- Copy and content: passed; Discovery, all four LAB services, descriptive copy, and Appearance remain complete.
- Interaction: passed; pointer hover adds a visible branded underline without introducing a fill, keyboard focus uses the same treatment, Escape closes the sheet, and trigger focus is restored.
- Comparison history: the supplied state had unnecessary row fills and true 16px content overflow caused by a full-width separator plus horizontal margins (P1). Neutral interaction states, a bounded scroll axis, and parent-aware separator sizing remove both issues with no remaining P0/P1/P2 difference.

### LAB collection taxonomy — 2026-08-13

- Source visual truth: the four user-supplied Skill/Recipe breadcrumb and eyebrow captures from 2026-08-13, which fused the organization and collection into `LAB Skills` or `LAB Recipes` and began detail breadcrumbs with an orphan separator.
- Implementation evidence: `.artifacts/detail-taxonomy-skill-full.png` and `.artifacts/detail-taxonomy-recipe-full.png`; focused crops are stored beside them.
- Normalized comparison: `.artifacts/detail-taxonomy-comparison.png` places both supplied detail states beside their implementations at matched scale.
- Focused comparison: passed; both detail families now render `LAB → collection → detail`, with no leading separator and with the current detail as the terminal item.
- Typography and spacing: passed; existing LAB type tokens, separator icon, compact rhythm, and wrapping behavior are preserved.
- Colors and visual tokens: passed; breadcrumbs and eyebrows inherit the owning page theme without introducing a second accent.
- Copy and content: passed; collection pages use `LAB / Skills / …` and `LAB / Recipes / …`, and detail eyebrows follow the same taxonomy.
- SEO and semantics: passed; visible breadcrumbs and JSON-LD BreadcrumbList data expose the same three-level organization, collection, and detail hierarchy.
- Interaction: passed; `LAB` resolves to `lab.sa`, collection links use base-aware local routes, and both detail pages report two separators and no horizontal overflow.
- Comparison history: the supplied state had one fused taxonomy label and an orphan leading separator (P2). The shared breadcrumb and hero composition now expose separate organization and collection levels with no remaining P0/P1/P2 difference.

### Gradual Package contents fade — 2026-08-13

- Source visual truth: `/var/folders/sl/87qshknd4gz8bg33zgm4g_300000gn/T/TemporaryItems/NSIRD_screencaptureui_uAg9o0/Screenshot 2026-08-13 at 2.01.24 AM.png`, showing a long package inventory whose collapsed edge faded across roughly one row.
- Implementation evidence: `.artifacts/package-fade/implementation-focused.png`; the matched source/implementation stack is `.artifacts/package-fade/comparison.png`.
- Focused comparison: passed; the fade now starts across the final three visible rows and progressively removes rules, labels, and icons into the real owning surface.
- Colors and themes: passed; the implementation remains a transparent alpha mask rather than a painted gradient, so it inherits every Skill brand and light/dark surface without color drift.
- Spacing and geometry: passed; the 420px collapsed reading window, parent-owned perimeter, row sizes, and centered disclosure action remain unchanged, with no page-level horizontal overflow.
- Interaction: passed; expanding reveals all 13 files with no mask, collapsing restores the 176px fade, and browser diagnostics contain no warnings or errors.
- Accessibility: passed; the shared disclosure continues removing clipped links from the tab order and restores them when expanded.
- Comparison history: the supplied edge became transparent too late and therefore appeared abrupt (P2). A component-level fade-depth option extends only this dense inventory's mask while preserving the established default elsewhere, with no remaining P0/P1/P2 difference.

### Detail breadcrumb and eyebrow separation — 2026-08-13

- Source visual truth: `/var/folders/sl/87qshknd4gz8bg33zgm4g_300000gn/T/TemporaryItems/NSIRD_screencaptureui_QjWtDq/Screenshot 2026-08-13 at 2.09.50 AM.png`, showing `LAB / Recipes` repeated in the breadcrumb and the adjacent hero eyebrow.
- Implementation evidence: `.artifacts/detail-eyebrow/recipe-focused.png`; the matched source/implementation stack is `.artifacts/detail-eyebrow/comparison.png`.
- Focused comparison: passed; the single navigational line remains `LAB → Recipes → Functioning Prototype`, while the eyebrow now reads only `Product delivery / Recipe`.
- Cross-family consistency: passed; Skill detail pages retain one `LAB → Skills → detail` breadcrumb and use the independent `category / Skill` classification eyebrow.
- Typography, spacing, and color: passed; existing breadcrumb links, separators, uppercase eyebrow tokens, vertical rhythm, and page-owned accent remain unchanged.
- Semantics and SEO: passed; the breadcrumb stays a single navigation landmark and retains the complete three-level JSON-LD hierarchy; the visual eyebrow is non-navigational classification copy.
- Browser verification: Recipe and Skill pages each report exactly one breadcrumb landmark, their expected distinct eyebrow, no horizontal overflow, and no warning or error diagnostics.
- Comparison history: the source state visually presented two near-identical taxonomy trails (P2). Separating navigation from classification removes the redundancy without discarding useful context, with no remaining P0/P1/P2 difference.

### Compact Recipe command truncation — 2026-08-13

- Source visual truth: `/var/folders/sl/87qshknd4gz8bg33zgm4g_300000gn/T/TemporaryItems/NSIRD_screencaptureui_1MOq1n/Screenshot 2026-08-13 at 2.12.34 AM.png`, showing nested horizontal scrollbars inside every compact requirement command.
- Implementation evidence: `.artifacts/recipe-command-clamp/implementation-focused.png`; the normalized source/implementation stack is `.artifacts/recipe-command-clamp/comparison.png`.
- Focused comparison: passed; all eight compact cells remain one line, overflow ends in a visible ellipsis, and the nested scrollbar tracks are gone.
- Layout and responsiveness: passed at the supplied two-column width; the cards report equal 738px client and scroll widths and the page has no horizontal overflow.
- Content preservation: passed; each visible label keeps its complete command in DOM text and its title attribute, while CopyButton still receives the unmodified source string.
- Accessibility and interaction: passed; clipped code is no longer a misleading tab stop, the copy action remains independently keyboard accessible, and the verified copy control reaches its success state.
- Cross-content behavior: passed; the non-installable Codex instruction uses the same one-line clamp without adding an unnecessary copy action.
- Browser verification: all eight command elements report `overflow-x: hidden`, `text-overflow: ellipsis`, `white-space: nowrap`, no tab stop, and no warning or error diagnostics.
- Comparison history: the supplied compact layout reused a horizontally scrollable code treatment intended for deliberate code exploration (P1). A compact-only truncation treatment removes the nested scroll affordance while preserving the complete command, with no remaining P0/P1/P2 difference.

- P0: none.
- P1: none.
- P2: none.
- P3: none that block fidelity, navigation, accessibility, or publication portability.

final result: passed
