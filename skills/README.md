# Stable skills

This directory is the public, installable catalog.

Each skill lives directly under `skills/<skill-name>/` and contains a root `SKILL.md`. Supporting references, scripts, assets, and evaluation cases stay inside that same directory.

Browse the generated [skill catalog](../docs/catalog.md) for the current collection.
